<% secrets("/teams/").each do |secretKey| %> * <%=secretKey%>
<% end %>

test/foo :<% secret("test/foo").each do |k, v| %>
  <%=k%>: <%=v%><% end %>


<!--<%#def recurseListing(prefix)-->
    a = {}
    secrets(prefix).each do |key|
      if key[-1] == '/'
        a[key] = recurseListing(prefix + "/#{key}")
      end
    end
  a
<!--  end-->
%>

<%#=recurseListing "teams/"%>
